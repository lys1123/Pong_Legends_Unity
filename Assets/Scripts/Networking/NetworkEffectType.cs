namespace PongLegends
{
    // Identifies which paddle ability visual to apply on remote machines.
    public enum NetworkEffectType
    {
        Freeze,
        SilentFreeze,
        Shrink,
        Slow,
        InvertControls
    }
}
